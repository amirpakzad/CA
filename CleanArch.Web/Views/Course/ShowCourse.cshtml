@model CleanArch.Application.ViewModel.ShowCourseViewModel
@{
    ViewData["Title"] = "ShowCourse";
}

<div class="container">
    <div class="row">

            <input type="hidden" value="@Model.ID" />
                <div class="col-lg-4">
                    <img class="img-thumbnail" src="@Model.ImageUrl" alt="." />
                </div>
                <div class="col-lg-8">
                    <div class="col-lg-12">@Model.Name</div>
                    <div class="col-lg-12">
                        <article>
                            @Model.Description
                        </article>
                    </div>
                </div>

    </div>
</div>
